# === 個人用の起動ファイル（公開しない）===
# 日本語ファイル名は非日本語環境で文字化け・起動失敗の恐れがあるため配布しない。
# 配布用には中身が同じローマ字の run.bat を使うこと。手元用としてのみ残す。
VRAMaぴょん起動.bat

# === アプリが書き出す記録ファイル（公開しない）===
# 📸記録／自動記録で作られるユーザーデータ。配布物には含めない。
VRAMaぴょん_記録*.xlsx
VRAMaぴょん_記録*.csv
*_記録_*.xlsx
*_記録_*.csv

# === Python が自動生成するファイル ===
__pycache__/
*.pyc
*.pyo

# === 開発用の使い捨てファイル ===
_*.png
_*.log
_test_*.py
run_log.txt

# === Office / エディタ／OS が作る雑多なファイル ===
~$*
*.tmp
.vscode/
.idea/
Thumbs.db
desktop.ini

# ◆始まり = 公開しない（自分用メモ・実験Ver）。物理的にpush不可にする
◆*
